#include "QuickSort.h"

std::vector <int> QuickSort::sort (std::vector<int> list) {

}

void QuickSort::quicksort (std::vector <int>& list) {}

int QuickSort::partition(std::vector<int>& list ,int low, int high) {
    int size = list.size();
    if (size < 3) {
        return;
    }
    
}